#include "LinkedList_queue.cpp"

int main()
{
    Queue q;
    q.enqueue(1);
    q.enqueue(2);
    q.enqueue(5);
    q.enqueue(8);
    //q.traverse();
    /*
    q.get_front();
    q.get_front();
    q.get_rear();
    q.get_rear();
    q.dequeue();
    q.traverse();
    q.dequeue();
    q.traverse();
    */

    return 0;
}

